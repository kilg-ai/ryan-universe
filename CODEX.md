# Codex share log — Ryan the 1 Universe Control Tower

Open copy for Codex. **No secrets.** Operator tokens, service keys, and private desk links are not in this repo.

Full workspace log (local): `C:\Users\ronki\AI-OPS\ryan-the1\artifacts\agent-work\CODEX-LOG.md`  
Workspace: `C:\Users\ronki\AI-OPS\ryan-the1`  
This repo: https://github.com/kilg-ai/ryan-universe  
Live UI: https://kilg-ai.github.io/ryan-universe/

Not Ron P&L. Keep BadinLake out. Do not fold in 28212 Watch. Do not replace https://ryanthe1.com.

## Live

| Piece | URL / place | Status |
|---|---|---|
| Control Tower | https://kilg-ai.github.io/ryan-universe/ | Live. GET 200. Title `Ryan the 1 Universe`. GitHub Pages from `gh-pages`. |
| Source | this repo, branch `main` | Vite app in repo root. Workspace copy lives at `web/`. |
| Booking desk | dedicated Supabase project `ifzztblqazgkyqbrybna`, function `booking-intake` | Active. Held replies only. No send from this UI. |
| ChatGPT Sites | `*.chatgpt.site` | Dead path. Do not redeploy. |

Open the live UI from the existing private operator link (`?k=`). Do not commit `k`, `.dev.vars`, or intake URLs.

## Stops until an explicit yes

Send customer mail, calendar writes, ChatGPT Sites, DNS / custom domain, replacing `ryanthe1.com`, 19U mail DNS, YouTube Studio, Ron’s Supabase project, BadinLake.

## Log

### 2026-09-09 — Off ChatGPT Sites onto GitHub

Ron asked for the Control Center live on our hosting/GitHub, not a ChatGPT Site.

- Vite Control Tower shipped in this repo.
- GitHub Pages is the host (`gh-pages`). No Actions workflow (token has no `workflow` scope).
- `netlify.toml` is ready for a later Netlify import. Not imported. No DNS.
- Live inbox uses the dedicated desk. Replies stay held.
- Do not rebuild a ChatGPT Site. Do not touch `ryanthe1.com` in this repo.

### How to append

Dated `### YYYY-MM-DD` block. Facts only. No tokens. Mirror the same fact into the workspace `CODEX-LOG.md` and `GROK-HQ\CURRENT.md`.
